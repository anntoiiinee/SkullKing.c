import * as React from "react";
interface SidebarContextValue {
    open: boolean;
    setOpen: React.Dispatch<React.SetStateAction<boolean>>;
}
declare function useSidebar(): SidebarContextValue;
export interface SidebarLayoutProps extends React.HTMLAttributes<HTMLDivElement> {
    /**
     * Height offset for a fixed navbar above the sidebar (e.g. 56 for h-14).
     * Controls `top` and `height` of the sidebar on desktop and mobile.
     * @default 56
     */
    navbarHeight?: number;
}
declare function SidebarLayout({ navbarHeight, className, children, ...props }: SidebarLayoutProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarTriggerProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
}
declare function SidebarTrigger({ className, ...props }: SidebarTriggerProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarProps extends React.HTMLAttributes<HTMLElement> {
    /**
     * Height offset matching the fixed navbar. Passed automatically
     * when used inside SidebarLayout, but can be overridden.
     * @default 56
     */
    navbarHeight?: number;
    /**
     * Visual theme of the panel.
     * - `"default"`: full-height panel flush against the edge with a right border.
     * - `"floating"`: detached card with margin all around, full rounded border and a soft shadow.
     * @default "default"
     */
    variant?: "default" | "floating";
}
declare function Sidebar({ navbarHeight, variant, className, children, ...props }: SidebarProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarInsetProps extends React.HTMLAttributes<HTMLElement> {
    /**
     * Height offset matching the fixed navbar for top padding.
     * @default 56
     */
    navbarHeight?: number;
}
declare function SidebarInset({ navbarHeight, className, children, ...props }: SidebarInsetProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarHeaderProps extends React.HTMLAttributes<HTMLDivElement> {
}
declare function SidebarHeader({ className, ...props }: SidebarHeaderProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarContentProps extends React.HTMLAttributes<HTMLDivElement> {
}
declare function SidebarContent({ className, ...props }: SidebarContentProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarFooterProps extends React.HTMLAttributes<HTMLDivElement> {
}
declare function SidebarFooter({ className, ...props }: SidebarFooterProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarGroupProps extends React.HTMLAttributes<HTMLDivElement> {
    /** Optional group label displayed above the items. */
    label?: string;
}
declare function SidebarGroup({ label, className, children, ...props }: SidebarGroupProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarSeparatorProps extends React.HTMLAttributes<HTMLDivElement> {
}
declare function SidebarSeparator({ className, ...props }: SidebarSeparatorProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarItemProps extends React.HTMLAttributes<HTMLElement> {
    /** Icon component rendered before the label. */
    icon?: React.ComponentType<{
        size?: number;
        className?: string;
    }>;
    /** Whether this item is currently active. */
    active?: boolean;
    /** Optional badge or indicator rendered on the right side. */
    badge?: React.ReactNode;
    /** Render as the child element (e.g., NavLink, anchor). @default false */
    asChild?: boolean;
}
declare function SidebarItem({ icon: Icon, active, badge, asChild, className, children, ...props }: SidebarItemProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarSubMenuProps extends React.HTMLAttributes<HTMLDivElement> {
}
declare function SidebarSubMenu({ className, ...props }: SidebarSubMenuProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarSubItemProps extends React.HTMLAttributes<HTMLElement> {
    /** Whether this item is currently active. */
    active?: boolean;
    /** Optional badge or indicator rendered on the right side. */
    badge?: React.ReactNode;
    /** Render as the child element (e.g., NavLink, anchor). @default false */
    asChild?: boolean;
}
declare function SidebarSubItem({ active, badge, asChild, className, children, ...props }: SidebarSubItemProps): import("react/jsx-runtime").JSX.Element;
export interface SidebarCollapsibleProps extends React.HTMLAttributes<HTMLDivElement> {
    /** Icon component rendered before the label. */
    icon?: React.ComponentType<{
        size?: number;
        className?: string;
    }>;
    /** Trigger label. */
    label: React.ReactNode;
    /** Optional badge rendered between label and chevron. */
    badge?: React.ReactNode;
    /** Whether the parent itself is in active state (e.g., on the matching route). */
    active?: boolean;
    /**
     * Initial open state. When omitted, the component auto-opens if any direct
     * child has `active={true}`.
     */
    defaultOpen?: boolean;
}
declare function SidebarCollapsible({ icon: Icon, label, badge, active, defaultOpen, className, children, ...props }: SidebarCollapsibleProps): import("react/jsx-runtime").JSX.Element;
export interface EasySidebarItem {
    /** Unique key for this item. */
    key: string;
    /** Display label. */
    label: string;
    /** Icon component. */
    icon?: React.ComponentType<{
        size?: number;
        className?: string;
    }>;
    /** Navigation href. Used with renderLink or as a plain <a>. */
    href?: string;
    /** Badge content rendered on the right side (string, number, or ReactNode). */
    badge?: React.ReactNode;
    /** Whether this item is currently active. */
    active?: boolean;
    /**
     * Optional sub-items rendered indented below this item.
     * Without `collapsible`, they are always visible (no caret).
     * With `collapsible`, the parent gets a chevron and toggles.
     */
    children?: EasySidebarItem[];
    /** When true and `children` is set, the parent toggles via a chevron. @default false */
    collapsible?: boolean;
    /**
     * Initial open state for collapsible items. When omitted, the parent
     * auto-opens if any child has `active: true`.
     */
    defaultOpen?: boolean;
}
export interface EasySidebarGroup {
    /** Group label displayed above the items. */
    label?: string;
    /** Items in this group. */
    items: EasySidebarItem[];
}
export type EasySidebarEntry = EasySidebarItem | EasySidebarGroup | "separator";
export interface EasySidebarProps extends Omit<React.HTMLAttributes<HTMLElement>, "children"> {
    /** Sidebar entries: items, groups, or "separator" strings. */
    items: EasySidebarEntry[];
    /** Header content rendered above the navigation. */
    header?: React.ReactNode;
    /** Additional CSS classes for the header wrapper. */
    headerClassName?: string;
    /** Footer content rendered below the navigation. */
    footer?: React.ReactNode;
    /**
     * Custom link renderer. Receives the item and must return a single element.
     * Used to integrate with react-router NavLink, Next.js Link, etc.
     * When provided, SidebarItem renders with asChild, merging its props onto
     * the element you return.
     *
     * @example
     * renderLink={(item) => <NavLink to={item.href}>{item.label}</NavLink>}
     */
    renderLink?: (item: EasySidebarItem) => React.ReactElement;
    /** Callback when an item is clicked. Receives the item key. */
    onNavigate?: (key: string) => void;
    /**
     * Height offset matching the fixed navbar.
     * @default 56
     */
    navbarHeight?: number;
    /**
     * Visual theme of the panel.
     * - `"default"`: full-height panel flush against the edge with a right border.
     * - `"floating"`: detached card with margin all around, full rounded border and a soft shadow.
     * @default "default"
     */
    variant?: "default" | "floating";
}
declare function EasySidebar({ items, header, headerClassName, footer, renderLink, onNavigate, navbarHeight, variant, className, ...props }: EasySidebarProps): import("react/jsx-runtime").JSX.Element;
export interface IconSidebarItemDef {
    /** Unique key for this item. */
    key: string;
    /** Icon component (required). */
    icon: React.ComponentType<{
        size?: number;
        className?: string;
    }>;
    /** Tooltip label shown on hover. */
    label?: string;
    /** Whether this item is currently active. */
    active?: boolean;
}
export interface IconSidebarProps extends React.HTMLAttributes<HTMLElement> {
    /** Array of icon items. */
    items: IconSidebarItemDef[];
    /** Callback when an item is clicked. Receives the item key. */
    onNavigate?: (key: string) => void;
    /** Header content rendered above the icons (e.g. app logo). */
    header?: React.ReactNode;
    /** Footer content rendered below the icons. */
    footer?: React.ReactNode;
}
/**
 * Narrow icon-only sidebar. Fixed width, no expand/collapse.
 * Each item shows only an icon with an optional tooltip on hover.
 *
 * @example
 * <IconSidebar
 *   items={[
 *     { key: "home", icon: Home, label: "Home", active: true },
 *     { key: "search", icon: Search, label: "Search" },
 *     { key: "settings", icon: Settings, label: "Settings" },
 *   ]}
 *   onNavigate={setPage}
 * />
 */
declare function IconSidebar({ items, onNavigate, header, footer, className, ...props }: IconSidebarProps): import("react/jsx-runtime").JSX.Element;
export { SidebarLayout, SidebarTrigger, Sidebar, SidebarInset, SidebarHeader, SidebarContent, SidebarFooter, SidebarGroup, SidebarSeparator, SidebarItem, SidebarSubMenu, SidebarSubItem, SidebarCollapsible, EasySidebar, IconSidebar, useSidebar, };
